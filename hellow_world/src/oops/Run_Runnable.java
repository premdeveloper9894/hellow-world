package oops;

public class Run_Runnable {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
     //Run() Method:
		   /*
		    * > Run() is a method defined in the Runnable interface.It is the  entry point for a
		    * thread execution. it doesn't creat a new thread if called directly instead it run the code on the
		    * current thread.
		    */
	//Runnable interface:
		  /*
		   * > Java Runnable is an interface used to execute code on concurrent thread.
		   * > it's provided a single method run(), which must be implementation.
		   */
		    
  // When use Runnable:
		   /*
		      > Java Support single inheritance , So if your class already extends another class , you can't extends
		      thread. that time use runnabale 
		      
		      > At a time i need 2 class and thread , then we sould go for Runnable interface.
		    */
		  
	}

}
